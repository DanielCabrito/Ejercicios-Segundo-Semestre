/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package com.daniel.ejercicios.bodymass;

/**
 *
 * @author carri
 */
public class BodyMassTest {
/*
    private BodyMass calculator;

    @Test
    void calculateBmi() {

        calculator = new BodyMass(80, 190);
        assertEquals(22.2, calculator.calculateBmi());

        calculator = new BodyMass(80, 150);
        assertEquals(35.6, calculator.calculateBmi());

        calculator = new BodyMass(55, 160);
        assertEquals(21.5, calculator.calculateBmi());
    }

    @Test
    void getBmiCategory() {
        calculator = new BodyMass(0, 0);
        assertEquals("delgado severo", calculator.getBmiCategory(0));
        assertEquals("delgado moderado ", calculator.getBmiCategory(16));
        assertEquals("delgado leve", calculator.getBmiCategory(17));
        assertEquals("normal", calculator.getBmiCategory(18.5));
        assertEquals("exceso", calculator.getBmiCategory(25));
        assertEquals("obeso clase 1", calculator.getBmiCategory(30));
        assertEquals("obeso clase 2", calculator.getBmiCategory(35));
        assertEquals("obeso clase 3", calculator.getBmiCategory(40));
    }
 */
}
